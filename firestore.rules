rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // dailyFortunes는 Admin SDK(Cloud Functions)만 접근 가능
    // 클라이언트 직접 읽기/쓰기 차단
    match /dailyFortunes/{docId} {
      allow read, write: if false;
    }
  }
}
